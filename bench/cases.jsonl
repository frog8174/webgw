{"id": "attn-mechanism", "fixture": "transformer.md", "query": "how does multi-head attention work", "expect": ["Attention head", "Multihead attention"], "reject": ["Subsequent work"], "note": "Regression: a 36-token section whose only body is an [edit] link ranked first live on 2026-09-04, because its title matched 'work' and BM25 length normalization inflates short documents. The two sections that answer the query were pushed out of the budget."}
{"id": "attn-scaled-dot", "fixture": "transformer.md", "query": "scaled dot product query key value softmax", "expect": ["Attention head"], "reject": ["Subsequent work"], "note": "Literal term match: BM25 should be strong here."}
{"id": "tokenizer", "fixture": "transformer.md", "query": "byte pair encoding vocabulary subword", "expect": ["Tokenization"], "reject": ["Subsequent work"], "note": "Unambiguous single-section answer."}
{"id": "why-parallel", "fixture": "transformer.md", "query": "why is training faster than recurrent models", "expect": ["Parallelizing attention"], "reject": ["Subsequent work"], "note": "Paraphrase: the section says 'training slow' and 'processed simultaneously' but never 'faster'. BM25 is expected to struggle; this is what reranking is for."}
{"id": "chrome-immunity", "fixture": "transformer.md", "query": "edit this page view history printable version", "expect": ["(preamble)"], "reject": [], "note": "A chrome-only query should rank the chrome section first rather than a content section. It does not assert that content stays out of the budget: this fixture is ~1k tokens against an 8k budget, so everything fits, and that is correct behaviour rather than a defect."}
